package fleetBuilder.temporary
/*
import com.fs.starfarer.api.ui.CustomPanelAPI
import fleetBuilder.ui.PopUpUI.BasePopUpUI

class ExampleDialog(
    override var headerTitle: String? = null
) : BasePopUpUI() {
    override fun createContentForDialog() {
        val tooltip = this@ExampleDialog.panel!!.createUIElement(this@ExampleDialog.panel!!.position.width - 30, this@ExampleDialog.panel!!.position.height - y, true)
        this@ExampleDialog.panel!!.addUIElement(tooltip).inTL(x, y)

        tooltip.setParaInsigniaLarge()
        tooltip.addPara("Test message 1", 5f)
        tooltip.addPara("Test message 2", 5f)

        createConfirmAndCancelSection(addCancelButton = false)
    }

    override fun applyConfirmScript() {

    }
}

 */